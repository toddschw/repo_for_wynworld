<% provide(:title, "Add New Company") %>

<h1>Add New Company</h1>

<%= render 'form' %>

<div class="float-right">
  <%= button_to "cancel", companies_path, :class => "menu-btn", :method => :get%>
</div>
