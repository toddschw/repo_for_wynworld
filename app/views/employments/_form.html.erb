<%= form_for(@user.employments) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Salary" %><br>
    <%= f.text_field :fname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
